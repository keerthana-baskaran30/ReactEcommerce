import Product from '../components/Product'

const ProductList = (props) => {
    return (
        <>dcdc
            {props.props}
            <Product filter = {props.props}/>
            {/* <Link to = "/mensclothing"/> */}
        </>

    );
};

export default ProductList;